/* ============================================================
   Matching — public surface
   Idara eligibility is a gate, not a score; see matcher.ts.
   ============================================================ */

export * from "./types";
export { rankForShift } from "./matcher";
export type { MatchInput } from "./matcher";
